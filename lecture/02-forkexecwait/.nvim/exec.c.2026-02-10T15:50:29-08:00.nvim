#include <stdio.h>
#include <sys/types.h>
#include <unistd.h>

int main() {
  pid_t pid = fork();
  if (pid != 0) { // child
    execl("bin/ls", "bin/ls","-a", "-l", "-h");
  } else {
    execl("bin/ls", "bin/ls","-a");
  }
  printf("Done\n");
}
