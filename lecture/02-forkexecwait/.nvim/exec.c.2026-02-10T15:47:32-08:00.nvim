#include <stdio.h>
#include <unistd.h>
#include <sys/types.h>

int main() {
  pid_t pid = fork();
  if (pid != 0) { //child
    execl("bin/ls", "-a", "-l", "-h");
  }
  else {
    execl("bin/ls", "-a");
  }

}
